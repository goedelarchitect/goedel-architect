

# Additional context for refinement

You are being given a previous attempt at this proof along with structured grader feedback identifying its issues. Your task is to produce a new proof that ADDRESSES every issue identified by the grader while still satisfying all the requirements above (anchored to the formal statement, no forbidden phrases, every step shown, every hypothesis cited, structured as atomic self-contained lemmas).

Specifically:
- Do NOT simply repeat the previous attempt — fix the issues the grader identified.
- Do NOT introduce new issues of the same kind the grader flagged (e.g. if `rigor_issues` says "the case k=0 is undefended", make sure the new proof covers k=0 explicitly).
- If the grader flagged `decomposability_issues` (a monolithic lemma, implicit gluing, unstated hypotheses), RESTRUCTURE: split the offending lemma into several small self-contained lemmas, cite cross-references by number, and state each lemma's hypotheses explicitly. This is the most common reason a rigorous proof fails to formalize.
- Do NOT abandon the parts of the previous attempt that the grader did not flag — those parts may be reused, but every step must still meet the requirements.
- If the grader's `final_verdict` is `WRONG_STRATEGY`, you may pursue a different approach; otherwise, repair the existing strategy.
